= form_for @plaent_tag do |f|
  - if @plaent_tag.errors.any?
    #error_explanation
      %h2= "#{pluralize(@plaent_tag.errors.count, "error")} prohibited this plaent_tag from being saved:"
      %ul
        - @plaent_tag.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :planet_id
    = f.number_field :planet_id
  .field
    = f.label :tag_id
    = f.number_field :tag_id
  .actions
    = f.submit 'Save'
